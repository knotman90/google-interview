#include <iostream>
#include <vector>
using namespace std;

int main(){
	using vector<int> = vi;
	int n = 0;
	int m = 0;
	cin>>n>>m;

	vi v;
	vi even, odd;
	v.push_back(0);
	even.push_back(0);
	odd.push_back(0);
	int nn =n;
	for(nn--){
		int a; cin>>a;
		v.push_back(a);

	}

	
	
}